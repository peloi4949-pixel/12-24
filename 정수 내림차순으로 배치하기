#include <string>
#include <vector>
#include <algorithm>

using namespace std;

long long solution(long long n) 
{
    string s = to_string(n);
    
    // 내림차순 정렬
    sort(s.begin(), s.end(), greater<char>());
    
    // 문자열
    return stoll(s);
}
